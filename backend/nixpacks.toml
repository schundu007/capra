[phases.setup]
nixPkgs = ["nodejs_20", "bash", "sqlite", "gcc"]
aptPkgs = ["python3-full", "python3-pip"]

[phases.install]
cmds = ["npm install", "pip install --break-system-packages requests numpy pandas httpx aiohttp beautifulsoup4 lxml"]

[start]
cmd = "node src/index.js"
